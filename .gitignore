__pycache__/
*.pyc
.DS_Store

# Never commit anything derived from a real corpus. These files contain the
# user's own prompts verbatim. Whole directories, not named files — a new file
# dropped in here must not become a leak.
eval/
reports/
*.corpus.json
corpus.json
